import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const RouteBusList = (values: any) => {
  console.log(values);

  return (
    <View>
      <Text>RouteBusList</Text>
    </View>
  );
};

export default RouteBusList;

const styles = StyleSheet.create({});
